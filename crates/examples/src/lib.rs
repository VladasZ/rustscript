//! This crate has no code of its own. It exists only to hold the rustscript
//! example scripts under `examples/`, so cargo can compile them as examples
//! while rustscript runs the same files as scripts.
