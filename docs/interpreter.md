# How the interpreter works

`rust FILE.rs` parses the source, compiles it to bytecode in memory and runs
it on a register VM. Cargo and `rustc` are not involved. The whole pipeline
takes milliseconds, so a script starts instantly.

## The pipeline

There are 4 steps, all in `crates/rustscript/src`.

1. The loader in `loader.rs` collects the files. `mod name;` loads `name.rs`
   or `name/mod.rs` like `rustc` does. Local path crates from the nearest
   `Cargo.toml` are grafted in as modules.
2. Every file is parsed with [`syn`](https://github.com/dtolnay/syn).
3. The resolver gives every item a key like `foo::bar` and resolves imports,
   renames, `crate::`, `self::`, `super::` and re-exports at load time.
4. The compiler in `interpreter/compile` lowers the AST into register
   bytecode once. Variables become register slots, control flow becomes
   jumps, patterns become test and bind ops, macros like `println!` are
   lowered inline. Types are lowered into a plain IR too.

The VM in `interpreter/vm.rs` runs one `Chunk` per function and never touches
the AST again. A variable read is an array read by register number. That is
where most of the speed comes from.

Names resolve to ids at compile time. Enums carry a shared `EnumDef` and a
variant index, so a match compares 2 integers. Struct field access caches its
slot per site. Method names are `BuiltinId`s generated by `build.rs` from
`method_names.txt`. Paths like `fs::read_to_string` are `PathId`s from
`path_names.txt`, matched by the longest suffix, so `std::fs::read_to_string`
is the same id. User `impl` methods are found by type id and method id in
`interpreter/impls.rs`. Unknown names go through a cold by name lookup. To
add a method or path, add its name to the table and the arm that implements
it.

## The runtime

Values are `Arc` with a `parking_lot` mutex, so they are `Send + Sync`. The VM
runs on a multi thread `tokio` runtime, so `tokio::spawn` and `.await` behave
like compiled Rust.

Composite values are copy on write. `clone` is a refcount bump and every
mutable access splits the value from sharing first. `Rc`, `Arc`, `RefCell`,
`Cell` and `Mutex` are real shared cells. `*guard += n` is one fused op that
holds the lock across the read-modify-write.

Strings are `Arc<String>` read lock free. `push` grows in place when the
buffer is not shared, so a build up loop stays linear.

Iterators are lazy native resources, so `by_ref`, `peekable` and open ranges
keep their real semantics.

Hot loops specialize into plans that run unboxed inside one dispatch. The
plan translates the body once and falls back to the generic path on any
surprise, with the registers restored to the start of the failing iteration,
so the output and the panic line never change.

- `sort_by` with an int only closure sorts on flat `i64` registers in
  `interpreter/scalar.rs`.
- Scalar `for` loops over ranges, `bytes()` or scalar vecs run in
  `interpreter/scalar_for.rs`. Vec pushes, map probes and inserts, string
  span keys from `split_whitespace` and `find_iter`, and json item reads run
  inside them. Writes go into a journal that is undone on failure.
- `sum`, `count`, `any` and `all` over `map` and `filter` stages run in
  `interpreter/scalar_chain.rs`. Stages are pure, so a failed run re-runs from
  the untouched source.
- `while` and `loop` regions run in `interpreter/scalar_while.rs`, with
  `v[i]` reads and writes and struct fields of vec elements.
- Self recursive scalar functions run whole in `interpreter/scalar_fn.rs`, on
  a flat frame stack. Recursive enums like a binary tree live in a run local
  table there.

A loop or function that does not qualify records it in one atomic flag, so
it pays one load and nothing else.

## No second type system

The interpreter does not type check. `rustc` does that in `rust check`. There
will never be a second Rust type system here. Lifetimes and generics mean
nothing at runtime.

## Numerics match debug Rust

An interpreted run must be byte for byte equal to a default `cargo run`,
panics included.

Values carry their real integer width, `u8` through `u64`, `usize`, `i8`
through `i64`, `f32`, `f64`. Overflow panics where debug Rust panics. `as`
truncates or saturates like `rustc`. `f32` computes at `f32` precision.
Integer methods answer in the receiver's width. Declared parameter and
return types retag values.

Result types chosen by the caller follow the source. `parse` and `sum` take
the turbofish, so `"300".parse::<u8>()` is an `Err`. `unwrap_or_default`
reads the nearest annotation, a `None::<T>`, a `let` type, a turbofish, a
closure parameter type or the return type of the function. `into()` picks the
`From` impl by the source type. A value that already is the target type
needs no conversion, so `?` on the same error type is identity.

Bare literals are typed by context like `rustc` does. A `let` annotation
reaches through `if`, `match`, negation, `vec!`, tuples and `Some`. A literal
with no other use is `i32`, so `{:x}` of `-1` shows 8 digits. A float `sum`
of nothing is `-0.0`.

`u128` and `i128` have real 128 bit storage and overflow checks, and their
methods and format specs answer correctly past `i64::MAX`.

## Traits

Trait impls register per concrete type and default bodies fill the gaps.
User `Display`, `Debug`, operator and `Iterator` impls drive `{}`, `{:?}`,
operators and for loops. Associated consts are `Type::NAME` globals. A trait
impl on a builtin type is keyed by the written type, so `Vec<u8>` and
`Vec<String>` keep separate bodies.

`Drop` runs where real Rust runs it. Scope end in reverse order, explicit
`drop`, each loop iteration, `break`, `continue`, `return`, `?` and panic
unwinding. A moved or shared value is dropped by its real owner, so an `Rc`
cycle leaks like in real Rust.

## Panics and errors

A panic prints the standard header with file and line, a script backtrace and
exits with 101. An `Err` out of `main` prints `Error: ...` and exits with 1.
Same as compiled Rust.

## Crate bridges

Dependencies are never compiled. `reqwest::get` or `Regex::new` dispatch to a
native bridge compiled into the `rust` binary. Method cores are written once
against plain Rust types, so a method cannot drift between call forms. A
crate without a bridge stops with `unsupported crate`.

## What `rust check` adds

`cargo check` proves the script is valid Rust. It cannot prove the bridge has
every method the script calls, and running only proves the lines that run.
So `interpreter/coverage.rs` walks the compiled bytecode. Every method call
is one op with a name, so every call is visible on every branch. Where the
receiver type is known the name is checked against that receiver. Path calls
like `std::process::exit` are a known gap.

The same walk runs before every interpreted run, so a script cannot die on a
cold branch after doing half its side effects. It costs less than
measurement noise.
